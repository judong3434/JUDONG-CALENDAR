# Studio

디자인 전공 대학생 1인용 일정·프로젝트 관리 웹.
한 줄 자연어 입력을 파싱해 일정·할 일·D-day 로 자동 변환하고,
열자마자 오늘 할 일과 진행 중인 프로젝트가 한 화면에 보이는 대시보드형 도구.

원본 기획: 바탕화면의 `일정관리 웹 1차 기획서 (가칭 Studio).docx`,
`바이브 코딩 프로젝트 브리프 (Studio).docx`

## 이 앱이 이겨야 할 상대

구글 캘린더가 아니라 **카카오톡 "나에게 보내기"의 입력 속도**다.
일정이 생기는 순간은 대부분 이동 중·수업 직후다. 10초 안에 던지고 끝나야 한다.
폼을 채우게 하면 그 순간 설계가 실패한 것이다.

## 설계 원칙 (기능 판단의 기준)

1. **입력은 한 곳, 한 줄로.** 날짜·시간·카테고리 고르는 폼을 먼저 보여주지 않는다.
   폼은 파싱 결과를 수정할 때만 등장한다.
2. **정리는 시스템이, 확인만 사람이.** 완벽한 파싱을 목표로 하지 않는다.
   파싱 실패는 오류가 아니라 Inbox 에 남는 정상 상태다.
3. **열면 다 보인다.** 정보를 탭 뒤로 숨기지 않는다. 홈에서 다 보여야 한다.
4. **프로젝트가 중심축이다.** 일정이 아니라 프로젝트가 최상위 단위다.
   "9월 12일에 뭐가 있지"보다 "졸전 지금 어디까지 왔지"가 더 자주 던지는 질문이다.

## 핵심 장치

Event 에 Task 가 매달리고, Task 는 실행할 날짜를 따로 갖는다.
입력 한 번으로 캘린더 · 오늘의 To-do · D-day 세 곳이 동시에 찬다.
이 구조에서 자동으로 나오는 것:

- 오늘의 To-do = `do_date` 가 오늘인 Task 전부
- D-day 스트립 = `is_dday = 1` 인 Event 를 가까운 순으로
- 프로젝트 진행률 = 완료 Task ÷ 전체 Task

## 스타일

**미니멀 · 낮은 채도 그레이스케일 · 높은 정보 밀도 · 색은 오직 분류를 위해서만**

- UI 전체는 무채색. 색을 쓰는 곳은 딱 세 군데 — 카테고리 점, 프로젝트 색, D-day 임박 경고.
- 색 값은 `src/lib/design/category.ts` 에서만 나온다. 여기 없는 색을 화면에 쓰지 말 것.
- 2단 색 체계: 색상(hue)은 카테고리가, 명도·채도(shade)는 개별 프로젝트가.
  프로젝트가 10개로 늘어도 화면은 6색으로 읽혀야 한다.
- 위계는 색이나 박스가 아니라 타이포 크기·굵기·여백으로.
- 4px 그리드 준수. 밀도는 높게, 여백은 균일하게.
- radius 는 6px 하나로 통일 (`rounded-c`).
- 모션 최소. 페이지 전환 애니메이션 없음. 상태 변화에만 150ms 이하.
- 아이콘은 단색 라인 아이콘 (Lucide). 이모지 아이콘 금지.
- 폰트 Pretendard.

### 하지 말 것

과한 그라데이션 · 글래스모피즘 · 그림자 남용 · 이모지 아이콘 · 둥근 모서리 남용 ·
파스텔 알록달록한 색면 · 채도 높은 배경색 · 일러스트 · 빈 화면의 캐릭터 ·
온보딩 튜토리얼 모달 · 정보를 탭 뒤로 숨기는 구조 ·
일정 하나 넣는 데 여러 단계 폼을 거치는 흐름 · FullCalendar 등 커스텀이 막힌 캘린더 라이브러리

## 기술 결정

| 영역 | 선택 | 비고 |
| --- | --- | --- |
| 프레임워크 | Next.js 16 (App Router) + TypeScript | |
| 스타일 | Tailwind CSS v4 | 토큰은 `globals.css` 의 `@theme` |
| DB | **libSQL (`@libsql/client`)** | 로컬은 파일, 배포는 Turso. 같은 코드 |
| 캘린더 | 직접 구현 (CSS Grid) | 라이브러리 금지 |
| 파싱 | 자체 규칙 파서 → LLM 폴백 | 대부분 규칙으로, 애매한 것만 AI |
| 모바일 | **반응형 웹** (PWA 아님) | 폰 브라우저로 그냥 열어서 본다 |

### DB 를 libSQL 로 한 이유

처음에는 Node 24 내장 `node:sqlite` 로 로컬 파일을 썼다. 계정 없이 바로 돌고
PC·폰이 같은 서버를 보므로 데이터가 하나였다. 그런데 Vercel 은 서버리스라
파일 시스템이 읽기 전용이고 요청마다 사라진다 — 로컬 파일로는 배포가 불가능하다.

libSQL 은 로컬 파일(`file:`)과 Turso(`libsql://`)를 같은 API 로 다룬다.
그래서 개발과 배포가 같은 코드로 돌고 주소만 달라진다. SQL 방언도 SQLite 그대로라
쿼리를 한 줄도 고치지 않았다. 대가는 모든 쿼리가 비동기가 된 것뿐이다.

- 환경변수가 없으면 `file:data/studio.db` (개발)
- `TURSO_DATABASE_URL` / `TURSO_AUTH_TOKEN` 이 있으면 그쪽 (배포)
- 배포 환경에서 URL 이 없으면 던진다. 조용히 빈 로컬 DB 를 보는 것보다 낫다

트랜잭션은 `batch()` 로 묶는다. 원격 DB 에서는 왕복 한 번으로 끝나고,
문장을 미리 다 만들어야 하므로 id 는 앱에서 생성한다(`newId`).

## 구조

```
src/app/            / 홈 대시보드, /calendar 캘린더, /timetable 시간표
db/migrations/     스키마. 파일명 순서가 곧 적용 순서.
data/              로컬 SQLite 파일 (gitignore). 개발 중에는 첫 쿼리 때 자동 마이그레이션.
scripts/           마이그레이션 러너, 데이터 이관. 앱 모듈 그래프에 기대지 않는다.
src/app/           라우트
src/components/    UI 컴포넌트
src/lib/db/        DB 커넥션 + 도메인별 쿼리
src/lib/parse/     Quick Capture 파서
src/lib/design/    카테고리·색 토큰
src/types/         도메인 타입 (DB 스키마와 1:1)
```

## 만드는 순서

- [x] 0. 스키마 + 프로젝트 구조
- [x] 1. Quick Capture + Inbox (파싱은 날짜/시간만)
- [x] 2. 홈 대시보드
- [x] 3. 월간 캘린더 + 색 체계
- [x] 4. 시간표
- [ ] 5. 파싱 고도화

**러닝 로그는 만들지 않는다.** 기획서 §4.5 와 브리프 「기능 7」에 있었지만 빼기로 했다.
스키마의 `log` 테이블은 쓰이지 않는다 — 지우려면 마이그레이션이 필요하고,
마음이 바뀌면 그대로 쓸 수 있어서 남겨 둔다.

**한 단계씩 진행하고, 각 단계가 끝나면 멈춰서 확인받을 것. 여러 기능을 한 번에 만들지 말 것.**
각 단계가 끝나면 실제로 며칠 써본다. 안 쓰게 되는 기능은 다음 단계에서 지운다.

## 개발

```
npm run dev        # 0.0.0.0 바인딩 — 같은 와이파이의 폰에서도 접속된다
npm run db:reset   # data/ 삭제. 다음 실행 때 스키마부터 다시 만든다
npm run db:migrate # 마이그레이션 적용. 배포 DB 에는 반드시 이걸로 (서버리스는 자동 적용 안 함)
npm test           # 파서·날짜 테스트 (node:test). 규칙을 고치면 반드시 돌릴 것
npm run smoke      # dev 서버가 뜬 상태에서 홈이 200 인지. SQL 오류는 빌드가 못 잡는다
```

{
  "name": "studio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "db:reset": "node -e \"require('fs').rmSync('data',{recursive:true,force:true});console.log('data/ 삭제됨')\"",
    "test": "node --test \"src/**/*.test.ts\"",
    "smoke": "node -e \"fetch(process.env.SMOKE_URL||'http://localhost:3000').then(r=>{console.log('status',r.status);process.exit(r.ok?0:1)}).catch(()=>{console.error('dev 서버가 안 떠 있습니다');process.exit(1)})\""
  },
  "dependencies": {
    "lucide-react": "^1.35.0",
    "next": "16.3.3",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^24.13.3",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
